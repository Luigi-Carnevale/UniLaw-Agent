[pytest]
# Configurazione test UniLaw Agent (FASE 1).
# I test girano offline: NON richiedono Ollama né l'indice ChromaDB.
testpaths = tests
addopts = -q --import-mode=importlib
filterwarnings =
    ignore
